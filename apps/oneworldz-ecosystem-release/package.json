{
  "name": "oneworldz-ecosystem-release",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/release.test.mjs",
    "verify": "npm run build && npm test"
  }
}
