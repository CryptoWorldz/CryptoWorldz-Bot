import { mkdir, readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { experienceContract } from "./experience-contract.mjs";

const root = path.dirname(fileURLToPath(import.meta.url));
const dist = path.join(root, "dist", "ecosystem");
const one = path.join(dist, "oneworldz");
const heroesDir = path.join(one, "heroes");
const jsDir = path.join(one, "assets", "js");
const escapeHtml = (v = "") => String(v).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;");

await mkdir(heroesDir, { recursive: true });
await mkdir(jsDir, { recursive: true });

const heroesPage = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#061b37"><title>OneWorldz Heroes | Real-World Action & Recognition</title><meta name="description" content="OneWorldz Heroes recognises verified real-world action, including work people were already doing before joining OneWorldz. Evidence is reviewed before public recognition."><meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1"><link rel="canonical" href="${experienceContract.heroes.publicUrl}"><link rel="stylesheet" href="/assets/css/site.css"><link rel="stylesheet" href="/assets/css/experience-theme.css"></head><body class="experience-theme theme-oneworldz"><a class="skip-link" href="#main-content">Skip to content</a><header class="site-header"><a class="site-brand" href="/"><span class="brand-profile text-brand"><b>O</b></span><span><strong>OneWorldz</strong><small>One Vision</small></span></a><button class="menu-button" type="button" aria-expanded="false" aria-controls="site-menu"><span></span><span></span><span></span><b>Menu</b></button><nav class="site-menu" id="site-menu"><a href="/">Home</a><a href="/heroes/" aria-current="page">Heroes</a><a href="/directory/">Directory</a><a href="https://cryptobotz.cryptoworldz.xyz/miniapp/">Command Centre</a></nav></header><button class="menu-backdrop" type="button" aria-label="Close menu" tabindex="-1"></button><main id="main-content"><section class="hero hero-recognition"><div class="hero-copy"><p class="eyebrow">REAL-WORLD HEROES</p><h1>Already helping people? That work matters.</h1><p>OneWorldz can recognise genuine real-world action that happened before or after someone joined the movement. Evidence is reviewed by people before public recognition. We do not invent impact, dates, endorsements or partnerships.</p><div class="button-row"><a class="button primary" href="https://t.me/CryptoWorldzBot?start=heroes" target="_blank" rel="noopener noreferrer">Submit Hero Evidence with ZED</a><a class="button secondary" href="https://cryptobotz.cryptoworldz.xyz/miniapp/#heroes" target="_blank" rel="noopener noreferrer">Open Hero Evidence in Command Centre</a></div></div></section><section class="section section-dark"><div class="section-heading"><p class="eyebrow">VERIFIED RECOGNITION</p><h2>Heroes are earned through evidence, not purchased.</h2><p>Donations, token holdings and follower counts do not buy Hero status. The public register below contains only approved recognition records.</p></div><div id="heroes-live" class="info-grid" aria-live="polite"><article><span>LOADING</span><h3>Checking the verified Heroes register…</h3><p>The page remains useful even if the protected register is temporarily unavailable.</p></article></div></section><section class="section"><div class="section-heading"><p class="eyebrow">HOW IT WORKS</p><h2>Evidence → Human Review → Recognition</h2></div><div class="info-grid"><article><span>01</span><h3>Tell the real story</h3><p>Describe what you actually did, where it happened and who it helped.</p></article><article><span>02</span><h3>Provide evidence</h3><p>Add a public HTTPS evidence link that an Admin can review.</p></article><article><span>03</span><h3>Human review</h3><p>No AI system auto-approves Hero recognition.</p></article><article><span>04</span><h3>Public recognition</h3><p>Approved evidence can become a verified OneWorldz Hero record.</p></article></div></section></main><footer class="site-footer vision-footer"><div><strong>Created with the Vision</strong><span>When Someone say's You can't Change the World 🌐 just Say “Why can't I?”</span></div></footer><script src="/assets/js/site.js" defer></script><script src="/assets/js/heroes.js" defer></script></body></html>`;

const heroesJs = `(() => {\n  const root = document.getElementById("heroes-live");\n  if (!root) return;\n  const escape = (v) => String(v ?? "").replace(/[&<>\"']/g, (c) => ({"&":"&amp;","<":"&lt;",">":"&gt;","\\\"":"&quot;","'":"&#39;"})[c]);\n  fetch("https://cryptobotz.cryptoworldz.xyz/api/public/heroes", { headers: { "accept": "application/json" } })\n    .then((r) => r.ok ? r.json() : Promise.reject(new Error("unavailable")))\n    .then((data) => {\n      const heroes = Array.isArray(data.heroes) ? data.heroes : [];\n      root.innerHTML = heroes.length ? heroes.map((hero, i) => '<article><span>HERO '+String(i+1).padStart(2,"0")+'</span><h3>'+escape(hero.display_name)+'</h3><p>'+escape(hero.summary)+'</p><small>Human-reviewed recognition</small></article>').join("") : '<article><span>REGISTER OPEN</span><h3>The Hero evidence pathway is live.</h3><p>Approved public Hero records will appear here after human evidence review.</p></article>';\n    })\n    .catch(() => { root.innerHTML = '<article><span>PROTECTED REGISTER</span><h3>Hero evidence remains available in the Command Centre.</h3><p>The public register could not be loaded right now. Recognition is never fabricated as a fallback.</p></article>'; });\n})();\n`;

await writeFile(path.join(heroesDir, "index.html"), heroesPage, "utf8");
await writeFile(path.join(jsDir, "heroes.js"), heroesJs, "utf8");

const homePath = path.join(one, "index.html");
let home = await readFile(homePath, "utf8");
if (!home.includes('id="heroes-pathway"')) {
  const section = `<section class="section" id="heroes-pathway"><div class="section-heading"><p class="eyebrow">ONEWORLDZ HEROES</p><h2>Already helping? Be recognised for real work.</h2><p>People do not have to start helping after joining OneWorldz. Existing genuine real-world action can be submitted with evidence for human review.</p></div><div class="button-row"><a class="button primary" href="/heroes/">Open OneWorldz Heroes</a><a class="button secondary" href="https://cryptobotz.cryptoworldz.xyz/miniapp/#heroes" target="_blank" rel="noopener noreferrer">Submit Evidence in Command Centre</a></div></section>`;
  const ack = home.lastIndexOf('<section class="section" id="acknowledgements">') >= 0 ? home.lastIndexOf('<section class="section" id="acknowledgements">') : home.lastIndexOf('<section class="section section-dark" id="acknowledgements">');
  if (ack < 0) throw new Error("OneWorldz acknowledgements insertion point missing for Heroes pathway");
  home = home.slice(0, ack) + section + home.slice(ack);
  await writeFile(homePath, home, "utf8");
}

console.log("OneWorldz Heroes experience built: public /heroes/ + protected evidence entry + human-review boundary.");
