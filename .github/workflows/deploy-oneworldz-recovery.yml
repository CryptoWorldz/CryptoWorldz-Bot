name: RETIRED — OneWorldz Recovery

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Block retired deployment path
        run: |
          echo '::error::This legacy OneWorldz recovery workflow is retired to prevent conflicting deployment logic and weak verification.'
          echo 'Use: Deploy Approved Worldz Site — Proof First -> target oneworldz'
          exit 1
