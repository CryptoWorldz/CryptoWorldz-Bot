name: RETIRED — Deploy Worldz Safe V2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Hard fail retired workflow
        shell: bash
        run: |
          set -euo pipefail
          echo '::error::Worldz Safe V2 is retired. Use the canonical proof-first deployment workflows.'
          exit 1
