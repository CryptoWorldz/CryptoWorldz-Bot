name: RETIRED — PDC HTTP Patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Block retired patch path
        run: |
          echo '::error::This ad-hoc PDC patch workflow is retired. Production PDC changes must use the dedicated proof-first standalone workflow with backup, exact image hashes and exact link checks.'
          echo 'Use: Deploy PurpleDiamondCrew Standalone'
          exit 1
