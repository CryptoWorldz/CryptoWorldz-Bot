name: RETIRED — Legacy Worldz Static Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Block retired deployment path
        run: |
          echo '::error::This legacy multi-site static deployment workflow is retired. It is forbidden because it can bypass the new exact-image, browser and rollback proof gates.'
          echo 'Use: Deploy Approved Worldz Site — Proof First, or the dedicated PDC/SolWorldz standalone proof workflow.'
          exit 1
