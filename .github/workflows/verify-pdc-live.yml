name: RETIRED — PDC Live Verifier

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Block contradictory verifier
        run: |
          echo '::error::This verifier is retired because it checked a different Zed-hosted route and could leave the custom-domain failure as a non-failing report.'
          echo 'Use the proof inside: Deploy PurpleDiamondCrew Standalone'
          exit 1
