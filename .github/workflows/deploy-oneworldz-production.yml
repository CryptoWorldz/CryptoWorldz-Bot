name: RETIRED — OneWorldz Production

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Block retired deployment path
        run: |
          echo '::error::This duplicate OneWorldz deployment workflow is retired to prevent conflicting deployment logic and false-success reporting.'
          echo 'Use: Deploy Approved Worldz Site — Proof First -> target oneworldz'
          exit 1
