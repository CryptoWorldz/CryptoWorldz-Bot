name: RETIRED — PDC Standalone Readiness

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Block stale readiness verifier
        run: |
          echo '::error::This readiness workflow is retired because it described obsolete vector fallbacks, only three pages, and could report readiness without proving the live site.'
          echo 'Use the proof inside: Deploy PurpleDiamondCrew Standalone'
          exit 1
