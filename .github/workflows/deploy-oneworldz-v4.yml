name: RETIRED — OneWorldz V4

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Block retired deployment path
        run: |
          echo '::error::This legacy OneWorldz V4 workflow is retired. Multiple competing production workflows were a root cause of inconsistent results.'
          echo 'Use: Deploy Approved Worldz Site — Proof First -> target oneworldz'
          exit 1
