name: RETIRED — EthWorldz Recovery

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Block retired deployment path
        run: |
          echo '::error::This legacy recovery workflow is retired because it allowed weak verification, insecure TLS bypass, and incomplete rollback proof.'
          echo 'Use: Deploy Approved Worldz Site — Proof First -> target ethworldz'
          exit 1
