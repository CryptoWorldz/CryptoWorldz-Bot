const tg = window.Telegram && window.Telegram.WebApp;
const state = { data: null, community: null };
const byId = (id) => document.getElementById(id);
const escapeHtml = (value) => String(value ?? "").replace(/[&<>'"]/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" })[character]);
const empty = (message) => `<div class="panel empty">${escapeHtml(message)}</div>`;
const formatDate = (value) => value ? new Date(value).toLocaleDateString() : "—";
const shortenAddress = (value) => value && value.length > 20 ? `${value.slice(0, 8)}…${value.slice(-8)}` : value;

async function api(path, options = {}) {
  const response = await fetch(path, { ...options, headers: { "Content-Type": "application/json", "X-Telegram-Init-Data": tg ? tg.initData : "", ...(options.headers || {}) } });
  const payload = await response.json().catch(() => ({ ok: false, error: "invalid_response" }));
  if (!response.ok) throw new Error(payload.error || "request_failed");
  return payload;
}

function missionCard(mission, current = false) {
  return `<article class="panel mission"><div class="mission-meta">${current ? "CURRENT RAAIIIDD • " : ""}MISSION #${escapeHtml(mission.id)}</div><h3>🎯 ${escapeHtml(mission.title)}</h3><div class="mission-meta">🌐 ${escapeHtml(mission.platform || "Community")} • ⭐ ${Number(mission.reward_points) || 0} Legend Points</div><p>${escapeHtml(mission.description || "Support this CryptoWorldz mission.")}</p><p class="instructions">${escapeHtml(mission.instructions || "Complete the mission and submit DONE.")}</p>${mission.expires_at ? `<small>Ends ${formatDate(mission.expires_at)}</small>` : ""}${mission.link || mission.target_url ? `<a class="button" href="${escapeHtml(mission.link || mission.target_url)}" target="_blank" rel="noopener">Open Mission</a>` : ""}<div class="mission-submit"><input type="url" placeholder="Optional HTTPS proof link" aria-label="Proof link"><button class="button submit-mission" data-mission-id="${escapeHtml(mission.id)}" type="button">Submit DONE</button></div></article>`;
}

function render() {
  const data = state.data;
  const profile = data.profile;
  byId("welcome-name").textContent = `Welcome, ${profile ? profile.first_name : data.telegram_user.first_name || "Legend"}`;
  byId("points").textContent = profile ? profile.points : "—";
  byId("rank").textContent = profile ? profile.rank : "Register First";
  byId("completed").textContent = profile ? profile.missions_completed : "—";
  byId("pending").textContent = profile ? profile.pending_submissions : "—";

  byId("current-mission").innerHTML = data.missions.length ? `<div class="section-title"><h2>🔥 Current Raaiiidd</h2></div>${missionCard(data.missions[0], true)}` : empty("No active Raaiiidd right now.");
  byId("mission-list").innerHTML = data.missions.length ? data.missions.map((mission) => missionCard(mission)).join("") : empty("No active Raaiiidds right now.");
  byId("home-rewards").innerHTML = `<div class="section-title"><h2>🎁 Recent Rewards</h2><button class="button secondary" data-open="rewards">View All</button></div>${renderRewards(data.rewards.slice(0, 3))}`;
  byId("reward-list").innerHTML = renderRewards(data.rewards);
  byId("leaderboard-list").innerHTML = data.leaderboard.length ? data.leaderboard.map((legend, index) => `<div class="leader"><span>${["🥇","🥈","🥉"][index] || index + 1}</span><span>${escapeHtml(legend.username ? `@${legend.username}` : legend.first_name || "Legend")}</span><strong>${Number(legend.points) || 0} LP</strong></div>`).join("") : empty("Leaderboard data is not available yet.");
  byId("profile-card").innerHTML = profile ? [["Telegram ID",profile.telegram_id],["Username",profile.username ? `@${profile.username}` : "Not Set"],["Rank",profile.rank],["Legend Points",profile.points],["Raaiiidds Complete",profile.missions_completed],["Pending",profile.pending_submissions],["Wallet",profile.wallet_connected ? profile.wallet : "Not Connected"],["Member Since",formatDate(profile.member_since)]].map(([label,value]) => `<div class="profile-row"><span>${escapeHtml(label)}</span><b>${escapeHtml(value)}</b></div>`).join("") + `<div class="panel security"><p>⚠️ Zed will never ask for your seed phrase or private key.</p></div>` : empty("Use /register with Zed before opening your Legend Profile.");
  byId("governance-list").innerHTML = data.governance.length ? data.governance.map((item) => `<article class="panel proposal"><h3>${escapeHtml(item.title)}</h3><p>${escapeHtml(item.description)}</p><small>Status: ${escapeHtml(item.status)}</small></article>`).join("") : empty("Community Governance is being prepared. No financial voting is active.");
  byId("kitty-list").innerHTML = data.treasury.length ? data.treasury.map((account) => `<article class="panel kitty-card"><h3>💰 ${escapeHtml(account.asset)} on Solana</h3><p>${escapeHtml(account.label)}</p><div class="kitty-address"><span>Public Address</span><code title="${escapeHtml(account.public_address)}">${escapeHtml(shortenAddress(account.public_address))}</code></div><label>Contribution Amount<input class="payment-amount" inputmode="decimal" type="number" min="0" step="any" placeholder="Optional amount"></label><img class="payment-qr" alt="${escapeHtml(account.asset)} payment QR" hidden><button class="button show-qr" type="button" data-asset="${escapeHtml(account.asset)}">Generate Secure QR</button><a class="button secondary wallet-link" href="#" data-address="${escapeHtml(account.public_address)}" data-asset="${escapeHtml(account.asset)}">Open Wallet</a><button class="button secondary copy-address" type="button" data-address="${escapeHtml(account.public_address)}" data-asset="${escapeHtml(account.asset)}">Copy Address</button><label>Transaction Signature<input class="claim-signature" placeholder="Paste signature after sending"></label><button class="button claim-contribution" data-asset="${escapeHtml(account.asset)}" type="button">Verify Contribution</button></article>`).join("") + `<div class="panel security"><p>Points are issued only after finalized on-chain verification and an enabled owner rule.</p><p>⚠️ Zed never asks for private keys and never moves funds automatically.</p></div>` : empty("The Community Kitty is being prepared. No contribution address is active yet.");
  renderCommunity();
  if (data.admin) { renderAdmin(); setTimeout(ensureOwnerTeamControls, 0); }
}

function renderRewards(rewards) { return rewards.length ? rewards.map((reward) => `<div class="reward"><span>⭐ ${escapeHtml(reward.reason || reward.reward_type || "Legend Reward")}</span><strong>${Number(reward.amount) > 0 ? "+" : ""}${Number(reward.amount) || 0}</strong></div>`).join("") : empty("No reward transactions yet."); }
function renderCommunity(){const links=state.community||{};const labels={telegram:"Telegram Community",x:"CryptoWorldz on X",announcements:"Announcements",support:"Support",website:"CryptoWorldz Website"};byId("community-links").innerHTML=`<div class="links">${Object.entries(labels).filter(([key])=>links[key]).map(([key,label])=>`<a class="link" href="${escapeHtml(links[key])}" target="_blank" rel="noopener">${escapeHtml(label)}</a>`).join("")}</div><button class="button secondary" data-open="kitty">Open Community Kitty</button><button class="button secondary" data-open="governance">Open Governance</button>${state.data.admin?'<button class="button secondary" data-open="admin">Open Admin Centre</button>':""}`;}
async function renderAdmin(){const access=state.data.admin_access||{};const rules=access.role==='owner'?`<form id="points-rule" class="panel"><h3>Contribution Points Rule</h3><select name="asset"><option>SOL</option><option>USDC</option></select><div class="form-row"><input name="points_per_unit" type="number" min="0" step="any" placeholder="Points per unit" required><input name="minimum_amount" type="number" min="0" step="any" placeholder="Minimum" required></div><input name="max_points" type="number" min="0" max="10000" placeholder="Maximum Points" required><label class="toggle"><input name="enabled" type="checkbox"> Enable verified auto-awards</label><button class="button">Save Rule</button></form>`:'';byId("admin-panel").innerHTML=`<div class="panel"><h3>Role: ${escapeHtml(access.role||"Admin")}</h3><p>${(access.permissions||[]).map(escapeHtml).join(" • ")}</p></div><form id="mission-create" class="panel"><h3>Create Raaiiidd</h3><input name="url" type="url" required placeholder="https://x.com/…"><div class="form-row"><input name="reward" type="number" min="0" max="10000" value="10"><input name="duration" placeholder="24h (optional)"></div><button class="button">Create Mission</button></form>${rules}<div id="admin-submissions" class="panel loading"><div class="orb"></div><p>Loading operations…</p></div>`;try{const [result,ops]=await Promise.all([api('/api/mini/admin/submissions'),api('/api/mini/admin/operations')]);byId("admin-submissions").outerHTML=`<div id="admin-submissions"><div class="panel"><h3>Command Centre Stats</h3><p>Legends ${ops.stats.users} • Wallets ${ops.stats.wallets} • Active ${ops.stats.active} • Pending ${ops.stats.pending} • Points ${ops.stats.points}</p></div>${result.submissions.length?result.submissions.map((item)=>`<article class="panel review-card"><b>Submission #${escapeHtml(item.id)} • Mission #${escapeHtml(item.mission_id)}</b><p>${escapeHtml(item.users?.username?`@${item.users.username}`:item.users?.first_name||item.telegram_id)}</p><p>${escapeHtml(item.proof_url||item.completion_text||"DONE")}</p><div class="form-row"><button class="button approve-submission" data-id="${item.id}">Approve</button><button class="button secondary reject-submission" data-id="${item.id}">Reject</button></div></article>`).join(""):empty("No pending submissions.")}<div class="panel"><h3>Team & Partner Foundation</h3><p>${ops.admins.length} active/configured team records.</p><p>${ops.partners.length ? `${ops.partners.length} approved partner profiles.` : "No partner profiles activated. Invitation-ready only."}</p></div></div>`;}catch{byId("admin-panel").innerHTML+=empty("Admin operations could not be loaded.");}}
function showScreen(id){document.querySelectorAll('.screen').forEach((screen)=>screen.classList.toggle('active',screen.id===id));document.querySelectorAll('.nav button').forEach((button)=>button.classList.toggle('active',button.dataset.screen===id));window.scrollTo({top:0,behavior:'smooth'});}
function feedback(message){if(tg&&tg.showAlert)tg.showAlert(message);else alert(message);}
function ensureOwnerTeamControls(){if(state.data.admin_access?.role!=="owner"||byId('admin-team'))return;const holder=document.createElement('div');holder.innerHTML=`<form id="admin-team" class="panel"><h3>Admin Team</h3><input name="telegram_id" inputmode="numeric" placeholder="Telegram ID" required><div class="form-row"><select name="role"><option>admin</option><option>moderator</option><option>recap_manager</option><option>partner_manager</option><option>treasury_manager</option></select><select name="status"><option>active</option><option>disabled</option></select></div><button class="button">Save Team Member</button></form><form id="admin-permission" class="panel"><h3>Permission Override</h3><input name="telegram_id" inputmode="numeric" placeholder="Telegram ID" required><input name="permission" placeholder="submission.approve" required><label class="toggle"><input name="enabled" type="checkbox" checked> Permission enabled</label><button class="button">Save Permission</button></form>`;byId('admin-panel').append(...holder.children);}
document.addEventListener('submit',async(event)=>{if(!['mission-create','points-rule','admin-team','admin-permission'].includes(event.target.id))return;event.preventDefault();const form=new FormData(event.target);const payload=Object.fromEntries(form);try{if(event.target.id==='mission-create'){await api('/api/mini/admin/missions',{method:'POST',body:JSON.stringify(payload)});feedback('✅ New Raaiiidd created.');}else if(event.target.id==='points-rule'){payload.enabled=form.has('enabled');await api('/api/mini/owner/contribution-rules',{method:'POST',body:JSON.stringify(payload)});feedback(`✅ ${payload.asset} contribution rule saved.`);}else if(event.target.id==='admin-team'){await api('/api/mini/owner/admins',{method:'POST',body:JSON.stringify(payload)});feedback('✅ Admin Team record saved.');}else{payload.enabled=form.has('enabled');await api('/api/mini/owner/permissions',{method:'POST',body:JSON.stringify(payload)});feedback('✅ Permission override saved.');}await renderAdmin();setTimeout(ensureOwnerTeamControls,0);}catch(error){feedback(`Update failed: ${error.message}`);}});
document.addEventListener('click',async(event)=>{const copy=event.target.closest('.copy-address');if(copy){try{await navigator.clipboard.writeText(copy.dataset.address);feedback(`✅ ${copy.dataset.asset} address copied.`);}catch{copy.textContent='Press and hold the address to copy';}return;}const submit=event.target.closest('.submit-mission');if(submit){const proof=submit.parentElement.querySelector('input').value.trim();try{const result=await api(`/api/mini/missions/${submit.dataset.missionId}/submit`,{method:'POST',body:JSON.stringify({proof_url:proof})});feedback(`✅ Submission #${result.submission.id} received for review.`);}catch(error){feedback(`Submission not accepted: ${error.message}`);}return;}const qr=event.target.closest('.show-qr');if(qr){const card=qr.closest('.kitty-card');const amount=card.querySelector('.payment-amount').value;const image=card.querySelector('.payment-qr');try{const response=await fetch(`/api/mini/kitty/qr?asset=${encodeURIComponent(qr.dataset.asset)}&amount=${encodeURIComponent(amount)}`,{headers:{'X-Telegram-Init-Data':tg.initData}});if(!response.ok)throw new Error();image.src=URL.createObjectURL(await response.blob());image.hidden=false;}catch{feedback('QR could not be generated.');}return;}const wallet=event.target.closest('.wallet-link');if(wallet){const amount=wallet.closest('.kitty-card').querySelector('.payment-amount').value;const params=new URLSearchParams({label:'CryptoWorldz Community Kitty',message:`CryptoWorldz ${wallet.dataset.asset} Community Kitty contribution`});if(amount)params.set('amount',amount);if(wallet.dataset.asset==='USDC')params.set('spl-token','EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v');wallet.href=`solana:${wallet.dataset.address}?${params}`;return;}const claim=event.target.closest('.claim-contribution');if(claim){const signature=claim.closest('.kitty-card').querySelector('.claim-signature').value.trim();try{const result=await api('/api/mini/kitty/claim',{method:'POST',body:JSON.stringify({asset:claim.dataset.asset,signature})});feedback(`✅ Verified ${result.verified_amount} ${result.asset}. ${result.contribution.points_awarded} Legend Points awarded.`);}catch(error){feedback(`Could not verify: ${error.message}`);}return;}const approve=event.target.closest('.approve-submission');if(approve){try{await api(`/api/mini/admin/submissions/${approve.dataset.id}/approve`,{method:'POST',body:'{}'});feedback('✅ Submission approved.');await renderAdmin();}catch(error){feedback(`Approval failed: ${error.message}`);}return;}const reject=event.target.closest('.reject-submission');if(reject){const reason=prompt('Rejection reason');if(!reason)return;try{await api(`/api/mini/admin/submissions/${reject.dataset.id}/reject`,{method:'POST',body:JSON.stringify({reason})});feedback('✅ Submission rejected.');await renderAdmin();}catch(error){feedback(`Rejection failed: ${error.message}`);}return;}const target=event.target.closest('[data-screen],[data-open]');if(target)showScreen(target.dataset.screen||target.dataset.open);});

async function start(){try{if(!tg||!tg.initData)throw new Error('telegram_required');tg.ready();tg.expand();tg.setHeaderColor('#09040f');tg.setBackgroundColor('#07030d');const [bootstrap,config]=await Promise.all([api('/api/mini/bootstrap'),fetch('/api/public/mini-config').then((response)=>response.json())]);state.data=bootstrap;state.community=config.community;render();byId('loading').classList.add('hidden');byId('content').classList.remove('hidden');byId('nav').classList.remove('hidden');byId('status').textContent='Secure';byId('status').classList.add('online');}catch(error){byId('loading').classList.add('hidden');byId('error').classList.remove('hidden');byId('error-text').textContent=error.message==='telegram_required'?'Open the Command Centre through Zed in Telegram.':'Zed could not securely open the Command Centre. Please try again.';byId('status').textContent='Locked';}}
start();
