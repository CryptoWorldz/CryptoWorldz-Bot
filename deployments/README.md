# OneWorldz Deployment Control

This directory is a **state/authority record only**. It is not a collection of workflow trigger files.

## Single static-fleet route

- Canonical source/build root: `apps/oneworldz-ecosystem-release/`
- Canonical topology and Hostinger destination contract: `apps/oneworldz-ecosystem-release/production-targets.mjs`
- Canonical verification: `npm run verify:web`
- Deployment law: `governance/JAYJAY-CHATGPT-DEPLOYMENT-GATE.md`
- Current owner authority: `Perform TOTAL DEPLOYMENT PLAN`
- Static production GitHub workflow: **NONE REQUIRED**
- Permitted production mechanism: **one authenticated Hostinger static-fleet rail only**
- Protected Node runtime: `cryptobotz.cryptoworldz.xyz` remains separate and is never a static target.

Only `oneworldz-19-total.request` is retained here. It records the current release state; changing it does not deploy anything.

## No parallel route rule

Do not recreate domain-specific request files, repair triggers, `*-only.request` files, alternate FTP routes, visual-repair triggers, deployment holds, fallback gateways or parallel production workflows.

Historical GitHub Environments may still exist as repository metadata. They are not deployment authority and must not be selected just because they exist.

## Hostinger destination proof

Before the first production write for the current canonical rail, authenticate every exact Hostinger website/account and record its real website home directory and credential scope.

A deployment remote path of `/` is valid only when the authenticated FTP/SFTP credential is already restricted to that exact website root. Hostinger may display the physical home directory as a path ending in `public_html`; that physical path is evidence, not a reason for the deployment process to prepend another `public_html` layer.

If the authenticated destination does not match the target, stop and correct the target contract. Do not probe alternative directories during production deployment.

## Current approval behaviour

JayJayTeamDev's current total-deployment instruction is standing authority for the unchanged canonical candidate chain. Do not request another approval phrase for the same release unless content, target mapping, payments, DNS/mail, protected runtime or security boundaries change.

Secrets never belong in this directory.
